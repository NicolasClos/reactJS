import React from 'react';
import '../../../styles/main/Ofertas.css';


const Ofertas = (props)=>{
    return(
        <div id={props.id}>
            <h3 className="ofertas">OFERTAS</h3>
        </div>
    );
}

export default Ofertas;
